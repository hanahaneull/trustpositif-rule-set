{
	"tasks": {
		"download": "deno run --allow-net --allow-write download.ts",
		"process": "deno run --allow-read --allow-write process.ts",
		"convert": "cd output && sing-box rule-set compile trustpositif-ip.json && sing-box rule-set compile trustpositif-domain.json",
		"start": "deno run download && deno run process && deno run convert"
	},
	"imports": {
		"@std/assert": "jsr:@std/assert@1",
		"tldts": "npm:tldts@^7.0.12"
	}
}
